name: Deploy Application (Docker + Helm)

on:
  push:
    branches:
      - main
      - 'feature/**'
    paths:
      - 'application/**'
  pull_request:
    branches:
      - main
    paths:
      - 'application/**'
      
  workflow_dispatch:
    inputs:
      action:
        description: 'Choose action to perform'
        required: true
        default: 'build_and_scan'
        type: choice
        options:
          - build_and_scan
          - deploy
      confirm_deploy:
        description: 'Check this box to confirm you want to DEPLOY to Kubernetes'
        required: true
        default: false
        type: boolean

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: wiz-demo-app
  CLUSTER_NAME: wiz-demo-cluster

jobs:
  # ---------------------------------------------------------
  # STAGE 1: SAST (Static Application Security Testing)
  # ---------------------------------------------------------
  sast-scan:
    name: Semgrep SAST Scan
    runs-on: ubuntu-latest
    
    # Set the working directory to the application folder
    defaults:
      run:
        working-directory: ./application
        
    steps:
      - uses: actions/checkout@v3
      - name: Run Semgrep SAST
        uses: returntocorp/semgrep-action@v1
        with:
          config: "p/default"
          auditOn: push 

  # ---------------------------------------------------------
  # STAGE 2: Build & SCA (Software Composition Analysis)
  # ---------------------------------------------------------
  build-and-sca:
    name: Build & Trivy SCA Scan
    needs: sast-scan
    runs-on: ubuntu-latest
    
    # Set the working directory to the application folder
    defaults:
      run:
        working-directory: ./application
        
    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Build Docker Image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # Build context is the current directory (./application)
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

      - name: Run Trivy Container Scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: '${{ steps.login-ecr.outputs.registry }}/${{ env.ECR_REPOSITORY }}:${{ github.sha }}'
          format: 'table'
          exit-code: '0' 
          severity: 'CRITICAL,HIGH'

      - name: Push Image to Amazon ECR
        if: ${{ github.event.inputs.action == 'deploy' }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

  # ---------------------------------------------------------
  # STAGE 3: Supply Chain Security (Signing & SBOM)
  # ---------------------------------------------------------
  supply-chain-security:
    name: Cosign Image Signing & SBOM Attestation
    needs: build-and-sca
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.action == 'deploy' }}
    
    defaults:
      run:
        working-directory: ./application
        
    steps:
      - uses: actions/checkout@v3
      - uses: sigstore/cosign-installer@v3.1.1
      - run: curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
        
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Sign & Attest SBOM
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          cosign sign --yes $REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          syft packages $REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -o spdx-json > sbom.json
          cosign attest --yes --predicate sbom.json --type spdxjson $REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

  # ---------------------------------------------------------
  # STAGE 4: Kubernetes Deploy
  # ---------------------------------------------------------
  deploy-to-eks:
    name: Deploy to EKS via Helm
    needs: supply-chain-security
    runs-on: ubuntu-latest
    
    if: ${{ github.event.inputs.action == 'deploy' && github.event.inputs.confirm_deploy == 'true' }}
    
    defaults:
      run:
        working-directory: ./application
        
    steps:
      - uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v1

      - name: Update Kubeconfig
        run: aws eks update-kubeconfig --name ${{ env.CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

      - name: Deploy via Helm
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          # Path to the helm chart is relative to ./application
          helm upgrade --install wiz-app ./helm-chart \
            --set image.repository=$REGISTRY/$ECR_REPOSITORY \
            --set image.tag=$IMAGE_TAG